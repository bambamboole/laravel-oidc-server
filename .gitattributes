* text=auto eol=lf

# Keep the Composer distribution lean: exclude everything that a consumer
# installing the package via `composer require` does not need at runtime.
/tests                  export-ignore
/.gitattributes         export-ignore
/.gitignore             export-ignore
